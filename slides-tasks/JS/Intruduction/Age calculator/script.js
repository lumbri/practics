
const birthYear = parseInt(prompt("Enter your birth year:"));
const futureYear = parseInt(prompt("Enter a future year:"));
const currentAge = futureYear - birthYear;
const nextAge = currentAge + 1;
alert(`I will be either ${currentAge} or ${nextAge} in ${futureYear}`);
