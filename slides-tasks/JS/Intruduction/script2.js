// Prompting for the name
let name = prompt("Please enter your name:");

// Checking if the entered name is "Liubov"
if (name === "Liubov") {
    // Printing a greeting using the name
    console.log("Hello, Liubov! Welcome!");
} else {
    // Printing a generic greeting
    console.log("Hello, " + name + "! Nice to meet you!");
}
